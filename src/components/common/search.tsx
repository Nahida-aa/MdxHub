import {Button as UIButton} from "@heroui/button";

export const SearchModal = ({ ...props }: React.ComponentProps<"button">) => {
  return <>
  <UIButton radius="full" variant="bordered">Open Modal</UIButton>
  </>
}