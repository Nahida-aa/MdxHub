---
title: md slug
description: 
created_at: 2025-03-17T18:03:47Z
updated_at: 2025-03-17T18:03:47Z
tags: [markdown, slug, url]
---

我们的文件来自与 各处: 本地\远程\数据库\文件系统\等等

我们的文件名包括文件夹名，可能是
```yml
- prefix/
  - index.mdx
  - 01-start.mdx
  - 02-install.mdx
  - 03-usage/
    - index.mdx
    - 01-contribution-guide.mdx
    - 02-advanced.mdx
```


```tsx  showLineNumbers fileame="src/app/docs/[...slug]/page.tsx"
export const generateStaticParams = async() => {
  return [{ slug: ['a', '1'] }, { slug: ['b'] }, { slug: ['c', '3'] }, { slug: ['d', '4', 'c'] }]
}

export default async function Page ({
  params,
}: {
  params: Promise<{ slug: string[] }>,
}) {
  const { slug } = await params
  
  return <Suspense fallback={<LoadingS />}>
    <h1>Page</h1>
    <p>This is the Page page.</p>
  </Suspense>
}
```