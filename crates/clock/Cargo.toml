[package]
name = "clock"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[lib]
doctest = false

[features]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
smallvec = "1.10"
