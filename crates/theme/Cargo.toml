[package]
name = "theme"
version = "0.1.0"
edition = "2021"

[lib]
name = "theme"
path = "src/theme.rs"

[dependencies]
gpui = "0.2"
gpui-component = { version = "0.5" }
anyhow = "1"
schemars = "1"
serde = { version = "1", features = ["derive"] }
